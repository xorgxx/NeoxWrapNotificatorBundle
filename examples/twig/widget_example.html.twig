{# examples/twig/widget_example.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Widget Notificateur (Exemple){% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Exemple d'utilisation du Widget</h1>
        <p class="text-muted">Cet exemple utilise la fonction Twig <code>{{ wrap_notify_form('type') }}</code> qui effectue une sous-requête vers le contrôleur dédié.</p>

        <div class="row mt-4">
            <div class="col-md-6">
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-primary text-white">Formulaire SMS</div>
                    <div class="card-body">
                        {{ wrap_notify_form('sms') }}
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-info text-white">Formulaire Email</div>
                    <div class="card-body">
                        {{ wrap_notify_form('email') }}
                    </div>
                </div>
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('notify_sms_form') }}" class="btn btn-outline-secondary">Voir le formulaire direct</a>
        </div>
    </div>
{% endblock %}
